import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },

  image: {
    height: 80,
    width: 80,
    borderRadius: 40,
  },
});

export default styles;
