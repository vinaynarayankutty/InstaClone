import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderRadius: 38,
    margin: 7,
    borderWidth: 3,
    borderColor: '#ae22e0',
  },

  image: {
    borderRadius: 35,
    borderWidth: 2,
    borderColor: '#fff',
  },
});

export default styles;
